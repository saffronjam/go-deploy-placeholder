name: kthcloud-ci
"on":
  push:
    branches:
    - main
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        registry: registry.cloud.cbh.kth.se
        username: robot$deploy-emils-deployment+emils-deployment
        password: Kaqux29h4cYbrI3uZohvCUrfqpmVk3DS
    - name: Build and push
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: registry.cloud.cbh.kth.se/deploy-emils-deployment/emils-deployment
