name: kthcloud-ci
"on":
  push:
    branches:
    - main
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        registry: registry.cloud.cbh.kth.se
        username: robot$deploy-deployme+deployme
        password: tIESeGzcjQFhJb4KG5AcQh4t0hy5mMcy
    - name: Build and push
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: registry.cloud.cbh.kth.se/deploy-deployme/deployme
